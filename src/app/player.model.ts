export class Character {
  constructor( public playerName: string, public name: string, public characterClass: number, public level: number, public background: string, public race: string, public alignment: string ) {}
}
